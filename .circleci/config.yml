version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-30-node
    environment:
      JAVA_HOME: /usr/lib/jvm/java-11-openjdk-amd64  # Adjust the path as per your Java installation
    steps:
      - checkout
      - run:
          name: Check Node.js Version
          command: node -v
      - run:
          name: Clear npm Cache
          command: npm cache clean --force
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run:
          name: Install Dependencies
          command: |
            npm install --verbose
            npm install react-native --verbose
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}
      - run:
          name: Build APK
          command: |
            cd android
            chmod +x gradlew
            ./gradlew assembleRelease
      - store_artifacts:
          path: android/app/build/outputs/apk/release/
          destination: release-apk/

workflows:
  version: 2
  build:
    jobs:
      - build
